<UserControl x:Class="MedML.View.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Пользователи" FontSize="24" VerticalAlignment="Center"/>
            <Button Content="Добавить" Margin="20,0,0,0" Padding="10,5" Click="AddUserButton_Click"/>
            <Button Content="Редактировать" Margin="10,0,0,0" Padding="10,5" Click="EditUserButton_Click"/>
            <Button Content="Удалить" Margin="10,0,0,0" Padding="10,5" Click="DeleteUserButton_Click"/>
            <Button Content="Обновить" Margin="10,0,0,0" Padding="10,5" Click="RefreshButton_Click"/>
        </StackPanel>
        <DataGrid Grid.Row="1" x:Name="UsersGrid" AutoGenerateColumns="False" Margin="10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" Width="60"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Path=Username}" Width="200"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Path=Role}" Width="120"/>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding Path=IsActive}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <TextBlock Text="Подсказка: пароли хранятся в виде хэша" VerticalAlignment="Center" Opacity="0.6"/>
        </StackPanel>
    </Grid>
</UserControl>
