<UserControl x:Class="MedML.View.LearnModelView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedML.View"
        xmlns:viewmodel="clr-namespace:MedML.ViewModel"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="600">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Обучение модели ML.NET" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>
        
        <!-- Основное содержимое -->
        <Border Grid.Row="1" 
                BorderBrush="#DDDDDD" 
                BorderThickness="1" 
                Padding="15" 
                Background="#F5F5F5" 
                CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Информация об обучении:" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,10"/>
                <TextBlock Text="Модель будет обучена на основе данных из файла heart.csv." 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="Процесс обучения может занять некоторое время в зависимости от объема данных." 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
        
        <!-- Статус обучения -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding StatusMessage}" 
                   FontWeight="SemiBold" 
                   Margin="0,20,0,10"/>
        
        <!-- Прогресс обучения -->
        <ProgressBar Grid.Row="3" 
                     Height="20" 
                     Value="{Binding Progress}" 
                     Margin="0,0,0,20"/>
        
        <!-- Кнопки управления -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Индикатор загрузки -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        Visibility="{Binding IsLearning, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Выполняется обучение..." 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
            </StackPanel>
            
            <!-- Кнопка обучения -->
            <Button Grid.Column="1" 
                    Content="Обучить модель" 
                    Command="{Binding LearnModelCommand}" 
                    Padding="15,10" 
                    Background="#007ACC" 
                    Foreground="White" 
                    BorderThickness="0"/>
        </Grid>
    </Grid>
</UserControl>
